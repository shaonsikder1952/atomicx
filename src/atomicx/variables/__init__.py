"""Variable Engine — computes, manages, and tracks all derived market variables.

This package provides:
- Base types for variable definitions
- Technical indicator computation (RSI, MACD, Bollinger, etc.)
- Advanced derived variables (funding rate term structure, order-book imbalance, etc.)
- Variable registry with metadata, weight history, and performance tracking
- Dynamic variable registration without code changes
"""
