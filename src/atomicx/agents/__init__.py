"""Hierarchical Agent Architecture — the core intelligence layer.

Agent hierarchy:
  Atomic Agents (1 per variable)
    → Group Leaders (aggregate 3-8 atomic agents)
      → Super Groups (synthesize group leaders)
        → Verification Layer (cross-check signals)
          → Common-Sense Layer (sanity tests)
            → Domain Leaders (domain-level confidence)
"""
